# 微风小镇 V1：验收、测试与发布清单

版本：0.3；日期：2026-09-08  
用途：定义“做出来”“测试通过”“可以交给测试者”之间的差别。

## 1. 证据原则

- 测试结果必须带游戏版本、规则/内容 hash、平台、运行模式、配置、日期（绝对日期）和日志位置。
- “可复现”至少意味着从空数据目录按步骤两次得到相同结论；随机 ID 可被测试夹具替换为固定 seed，但生产不能使用固定 seed。
- 自动测试证明规则；集成测试证明进程之间；手测证明可用性；性能测试证明给定环境下的目标。任何一类不能替代另一类。
- 失败用例要保留最小存档、输入序列、服务器日志和截图/录像（若涉及 UI），同时脱敏凭据和真实路径。
- 通过只指当次条件；改变代码、内容、规则、引擎、导出模板或网络适配器后必须重新跑受影响测试。

### 1.1 测试数据约定

测试用固定 fixture：

- `world_empty_v1`：未游玩新世界，第 1 日、资金 300、20 粒萝卜种子、4 株初始成熟萝卜、无贡献；“empty”不是空地图。
- `world_crop_boundary_v1`：一个即将成熟、一个当日已浇水、一个成熟作物、一个扩展区域边界。
- `world_stats_v1`：至少 3 名成员、并列分数、1 名撤销成员、1 名不同意公开成员。
- `world_save_generations_v1`：至少 5 个有效代、1 个损坏代、1 个未来 schema 代。
- `migration_world_v1`：包含成员身份、库存、资金、作物、日统计、建设和公开授权。

测试不得把真实用户世界或真实凭据纳入仓库。

## 2. CASE 用例

### A. 启动、世界和身份

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-01 | 空数据目录启动 → 创建世界 → 完成初始保存 → 退出；再建同名世界/尝试空白名称 | 世界名、world_id、版本和初始资金/物品正确；所有者本地凭据持久化且不进世界明文；同名不同 ID、空白拒绝；能再次加载 |
| CASE-02 | 单机创建 → 种植/跨日/收获/出售 → 正常退出 → 继续；M2 再用联机模式继续该世界 | 世界日、库存、资金、三榜、建设和所有者身份与退出前一致；切换启动模式不另建世界或重复发初始物品 |
| CASE-03 | A 开 listen_host → B 用正确连接卡 → 加密握手 → 新成员批准 | B 得到唯一稳定 player_id，完成快照后可操作；凭据不进日志 |
| CASE-04 | B 用错误地址/端口/证书/指纹 → 尝试加入 | 显示明确失败；不下发私有快照，不崩溃房主 |
| CASE-05 | 新成员超过历史 16 人或在线 4 人 → 尝试加入 | 在认证/批准前后均有正确拒绝；不覆盖已有成员 |
| CASE-06 | B 退出 → 再用原凭据加入；另一个客户端冒用 B 昵称 | B 恢复原背包/统计；冒用者不能认领 B |

### B. 移动、输入和可见性

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-07 | A/B 在地图移动、撞边界、互相穿过；UI 打开时按移动键 | 静态碰撞有效；玩家不互相堵塞；UI 输入不触发地面动作 |
| CASE-08 | 客户端发旧位置、超速位置、频率过高输入 | 服务器限速/纠偏/丢弃；不改变经济或权限；有受控警告日志 |

### C. 农务、容器和经济

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-09 | A 选择锄头整地→选快捷栏种萝卜→选水壶浇水→日切→空手收获；遍历五作物各生长日 | 一次点击只提交一次；水壶无需补水；作物阶段遵守 PRD 6.1；生长日、背包和各事件积分正确；仅有效操作计分 |
| CASE-10 | B 对同一未成熟作物同日浇水两次、成熟后再浇水 | 只有第一次产生 +1；后续无贡献/无副作用 |
| CASE-11 | A/B 同时收获同一成熟作物 | 恰好一个成功；另一个得到目标变化；总库存/收益不重复 |
| CASE-12 | 背包满、堆叠到 99、本人背包整理/拆分到快捷栏、仓库跨格转移、非法数量/他人容器 | 按目标格或自动入包规则完整接收请求，否则整笔失败；不静默缩减数量；同容器不重复扣加；非法不改状态 |
| CASE-13 | A/B 抢共享仓库最后物品，或同时放入最后空格 | 一个成功一个失败；容量、物品和回执一致 |
| CASE-14 | 资金 300 买种子、资金不足、负数/极大数量、改客户端价格、尝试出售种子/工具 | 合法采购按服务定义价格并入购买者背包；出售只接受作物产物；非法全部拒绝；余额不为负 |
| CASE-15 | C 出售 10 萝卜（24/个） → 查看资金/三个统计 | 资金 +240、C 累计/当日收益 +240、贡献不因出售增加；对账成立 |
| CASE-16 | 重发成功出售同一 `client_sequence`；同序号改 payload；跳号 | 原回执/拒绝/要求补序号；不发生第二次扣物或加钱 |

### D. 时间、建设和恢复经营

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-17 | 分别让出售在日切屏障前、屏障后提交；在23:59分别只播种/播种并浇水后日切；菜单/失焦/系统休眠后检查时间 | 旧日已提交销售只进旧日桶；新日无提交时为0，新日已提交销售按实际金额入新日桶；未浇水不长、已浇水长1日；菜单/失焦继续、系统休眠不补跑；无半日状态 |
| CASE-18 | 关闭所有玩家窗口/断开 → 等现实时间 → 重新加入 | 世界游戏日不推进；重连先同步再可操作 |
| CASE-19 | 捐项目达到需求、超过缺口、前置未完成、两人抢最后需求 | 请求超过当前缺口则整笔拒绝并提示缺口；合法笔完整接受；项目效果仅一次，积分按成功数量 |
| CASE-20 | 满足/不满足所有救济条件领取，重复点击或仓库满 | 仅精确满足条件成功；每世界每游戏日一次；失败不消耗资格/物品 |

### E. 榜单、公开授权和导出

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-21 | 3 名成员产生收益/贡献，含并列 → 打开三榜/明细 | 三榜定义、名次、并列、分类与游戏日说明准确；稳定顺序 |
| CASE-22 | 改名、重连、离线、撤销成员 → 查看榜单 | 同一 player_id 不新建行；撤销历史保留并标记；离线不清空 |
| CASE-23 | 统计 checkpoint + 后续事件恢复 → 对比实时物化统计 | 累计/当日/分类和对账一致；事件不会重复投影 |
| CASE-24 | 世界开关关、成员不同意、部分同意、改别名 → 预览导出 | 默认不公开；只出现同意行；使用公开别名/随机 ID；不泄露隐藏成员名次 |
| CASE-25 | 同一 revision 导出 HTML+JSON → 离线打开 HTML、解析 JSON | 字段/数值/名次一致；HTML 无网络请求、脚本、路径或原始 HTML 注入 |
| CASE-26 | 导出失败、撤回后再导出、用户输入恶意昵称/文件名 | 不覆盖上一有效文件；新文件不含撤回行；文本安全转义、固定文件名 |

### F. 保存、版本和迁移

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-27 | 自动保存、日切保存、正常退出、手动备份 → 查看 generations | 文件可解析且 checksum 正确；自动最多保留 5 代；手动备份不被删 |
| CASE-28 | 在临时写入、flush、发布指针各阶段注入失败 → 重启 | 上一有效代仍可加载；不出现一半物品/资金/统计；失败有提示 |
| CASE-29 | 破坏最新代、manifest、checksum → 选择上一有效代恢复 | 原件保留；提示回退时间/日；新 epoch，旧身份绑定和公开同意清空；维护恢复所有者后再绑定成员 |
| CASE-30 | 读 schema 0 fixture → 升级到 schema 1；读未来 schema/未知定义 | 合法迁移只发布新有效代；未来/未知拒绝并说明，不丢字段继续 |
| CASE-31 | 第二个房主实例启动；活跃实例崩溃后恢复 | 活跃锁拒绝第二实例；残留锁不会无条件删除；经过安全恢复可继续 |
| CASE-32 | 所有者关闭世界并成功保存 → 确认 A 已停服 → 导出私有迁移包 → dedicated 导入启动 → 原成员加入 | 拒绝从仍活跃的权威实例制作迁移包；世界/成员/库存/资金/榜单/日/建设一致；新 epoch/新证书提示正确 |

### G. 断线、专用服、UI 与发布

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-33 | B 在有未确认命令时断线 → 同 epoch 重连 | 先查询回执/同步；最多结算一次；确认后才恢复操作 |
| CASE-34 | B 断线超过 30 秒；A 正常继续/关闭 | B 回到手动重试；A 的世界持续/关闭符合规则；B 不能离线推进 |
| CASE-35 | A 突然终止 → B 等待 → A 用最近存档恢复 | B 获知结束；恢复可能回退但不发生本地分叉合并；重新同步 |
| CASE-36 | 以 dedicated/headless 启动同一世界 → client 加入并完成一项农务 | 无 UI/音频/房主角色依赖；与 listen_host 使用相同领域结果 |
| CASE-37 | 五名新手分别体验新世界：创建/加入→种植/浇水→收获/出售→榜单→退出 | 地址可达时5分钟内加入；入场10分钟内首次收获/出售（可用初始成熟作物）；15分钟内至少4/5完成整组操作，无开发者接管 |
| CASE-38 | 重绑定键位、中文昵称输入、窗口/全屏/UI 缩放、调音量 | 设置保存；输入焦点不误操作；文本和关键状态可读 |
| CASE-39 | 普通成员发送 owner 命令/修改 player_id/价格/分数/路径 | 全部拒绝；无状态变化；不崩溃，不把敏感细节回显 |
| CASE-40 | 超长/畸形/重复/乱序/超频消息和超大快照 | 有界拒绝/断开；进程继续服务其他玩家；内存和日志受限 |
| CASE-41 | 分别检查公开文件/日志、连接卡、私有迁移包 | 公开/日志不含凭据、IP、私有ID/背包/路径；连接卡可含地址/公开证书但无私钥；私有迁移包按白名单包含世界/摘要/必要私钥，禁止当公开包输出 |
| CASE-42 | 2–4人从新世界实际完成三项公共建设，记录每项完成时刻、人数、时间/资源并继续经营；单人另记 | 新世界至纪念碑完成以有效运行时间计，另记现实历时和中断；正常速率、不注入资源；三项目可达且完成后可玩；90–180分钟为调优目标，不强行截断测试或用 fixture 冒充首次推进 |
| CASE-43 | Windows/macOS 从干净目录构建并运行核心流程 | 版本、资源、存档目录和网络说明正确；无开发机绝对路径依赖 |
| CASE-44 | 打包包体、示例/空白世界、用户手册、版本变更日志 | 新用户可按文档创建/加入/备份；包哈希和素材许可已归档 |

### H. 补充关键边界

| ID | 场景与步骤 | 预期 |
|---|---|---|
| CASE-45 | 重绑/撤销 B 后旧凭据重连；丢失所有者凭据；再显式恢复撤销前备份 | 普通重绑旧凭据失效、身份/财产保留；所有者只能停服本机恢复；回退后全部旧绑定/公开同意失效，经核对重新绑定，不能复活已撤销者 |
| CASE-46 | 出售成功后立即崩溃；首次/周期/手动/控制事务保存明确失败；作业无结果且超35秒；退出或另存恢复包 | 崩溃回到完整快照且三榜/库存/资金一致；任何明确保存失败立即冻结，不等35秒；无结果超时也冻结；休眠不计35秒；失败候选身份/授权不激活；允许重试/另存，退出不伪报成功 |
| CASE-47 | 同凭据反复申请、8项待审批、60秒过期、拒绝、批准后回执丢失、同身份双连接 | 不重复占槽；仅保存成功批准占历史席位；查询恢复已确认身份；旧连接不能在新generation激活后提交；4人上限含房主 |
| CASE-48 | 公开包生成期间成员撤回、改公开别名/昵称或所有者关总开关；在复核与发布边界注入上述操作；HTML成功JSON失败 | 以控制事务屏障顺序裁定，撤回/关闭先提交则取消该发布；授权版本变化时取消/重生成；两文件成对成功或无新包；无脚本/网络请求；同名/特殊字符安全显示 |
| CASE-49 | LAN、真实异地可达公网IPv4、端口占用/防火墙拒绝、不可配置CGNAT、协议/内容不匹配 | 支持拓扑真实两机通过；不支持拓扑不伪称可连；15秒建连、60秒审批、10秒同步各自有界；版本不匹配在私有同步前拒绝 |

CASE-41 检查的是不同文件类别各自的数据边界，不能要求包含完整世界的私有迁移包“没有背包/私钥”。CASE-29/45 的回退安全恢复不同于 CASE-32 的正常迁移，后者保持已批准成员绑定。

各阶段只认领已实现的子场景，例如 M1 的 CASE-02 不包含 M2 的联机切换子场景。完整用例在 QA-02 汇总验收，不能以早期部分通过覆盖剩余断言。INT-03 的 30 分钟是合作并发回归的最短时段，CASE-42 是独立的完整推进验证，二者分别记录证据。

## 3. 自动化测试层级

### 3.1 单元与性质测试

覆盖：金额/数量边界、五种作物日切、贡献去重、对账不变量、项目缺口、榜单并列、公开白名单、命令序号、存档迁移、HTML 转义。

建议性质：

- 任意合法业务批次执行后所有不变量成立。
- 同一命令成功执行一次后重复执行不改变业务状态。
- 事件投影按事件顺序执行与 checkpoint + 后续执行结果一致。
- 公开快照只含白名单且不能通过任意字符串携带路径/脚本。
- 对任意失败业务批次，物品/资金/作物/建设/统计状态保持一致；允许已受理失败命令推进序号、失败回执和诊断计数，不能因比较运行期字段误判为业务半提交。

### 3.2 集成与多进程

- 启动房主进程和 1–3 个远端客户端进程（总玩家最多4人）；另测 dedicated 加4个客户端；测试固定握手、审批、快照、同步、命令回执和关闭。
- 记录每个进程的版本、随机种子（仅测试）、输入序列、revision、事件序列和退出码。
- 网络模拟覆盖延迟、丢包、重复、乱序、断开、重连；移动可降级，经济不能双结算。
- 存档测试在至少 4 个发布阶段注入失败：临时写、验证、发布、清理。

### 3.3 手测与可用性

QA 按 `CASE-03/15/21/24/27/32/37/43` 做完整录像/日志证据；新手不得接受开发者提示完成 CASE-37，问题写入反馈而不是现场修复后继续假装首测。

## 4. 性能与稳定性方案

### 4.1 基准记录

M0 登记：CPU/GPU/内存、OS 版本、分辨率、引擎版本、构建类型、网络仿真器、后台程序。M5 使用同一基准或明确标注变化。

### 4.2 场景

1. 本地 solo：256 株、连续 30 分钟。
2. 房主 + 3 客户端：移动和农务混合、256 株、连续 30 分钟。
3. 房主 + 3 客户端：100ms RTT/1% 丢包/20ms 抖动。
4. 房主 + 3 客户端：150ms RTT/3% 丢包/30ms 抖动。
5. 房主保存压力：30 秒自动保存、日切和手动备份交错；注入一次失败。
6. dedicated：无界面连续运行 2 小时，至少每 30 秒产生一项测试业务。

每项报告平均/P95/P99 帧耗时或延迟、峰值/平均带宽、RSS、保存耗时、错误率、重同步次数和断线次数。只报告 FPS 不够。

网络注入以 RTT 为口径，双向各施加目标 RTT 的一半基准延迟；丢包按每方向独立概率，抖动定义为每方向最大偏移（均匀分布）。报告实测 ping、实际工具参数与随机测试种子，不能把单向100ms配置写成100ms RTT。两档网络测试各至少持续30分钟。

### 4.3 稳定性门禁

- 30 分钟四人场景无崩溃、未处理异常、业务不变量破坏或可重复输入堆积。
- 2 小时 dedicated 在第10分钟预热后和最后10分钟窗口比较RSS中位数，增长不超过64MiB且整体≤1GiB；若M0基准调整阈值须记录依据。事件/日志维持规定上限。
- 关闭、断电模拟和恢复后，任何被判定“已保存”的快照可加载；未保存动作允许按 PRD 回退。

## 5. 安全与隐私检查

- [ ] 客户端不能决定 player_id、authority、价格、总额、贡献、游戏日或存档路径。
- [ ] 未认证/未批准连接不能读私有世界；成员不能执行 owner 命令。
- [ ] 凭据仅在受验证的加密连接上传；证书替换有明确失败路径。
- [ ] 所有消息字段先过类型、长度、范围、枚举和大小限制。
- [ ] 服务器不反序列化任意 Godot Object/Resource，不调用任意客户端提供的方法名。
- [ ] 重复/跳号/过期 epoch 请求不会重复业务；超频不会拖垮房主。
- [ ] 公开文件严格白名单；昵称/别名转义；不含连接资料、私有 ID、库存或路径。
- [ ] 日志、错误、诊断和崩溃文件不含明文凭据/私钥/完整认证数据。
- [ ] 迁移包拒绝路径穿越、绝对路径、符号链接和压缩炸弹式超限展开。
- [ ] 房主作弊边界写进用户说明，未宣称官方公平榜或不可篡改经济。

## 6. 缺陷分级与发布门

### Blocker：不能发布

崩溃/卡死导致无法创建、加入、继续或退出；任意重复结算、物品复制、负资金、身份串号；存档无法恢复或公开文件泄露凭据/私有数据；普通成员可执行 owner 命令；两个权威实例同时写世界。

### Critical：不能作为 0.1.0 成品发布

四人主要循环不可完成；频繁同步/重连后状态不一致；日切破坏收益/作物；目标平台无法启动；核心输入焦点误操作；公开授权撤回仍导出新数据；严重性能/内存/网络退化。

内部调试包可以带已知问题用于复现，但不能标记本 PRD 的 0.1.0 成品。数据/身份/授权问题无已知问题豁免。

### Major：默认延后发布但需要已知问题说明

非核心 UI、音效、帮助文本或边界体验问题；不能绕过核心循环、安全和数据完整性。M6 前要么修复，要么确认不阻断本轮封闭测试。

### Minor：可进入 backlog

视觉小瑕疵、非阻塞文案、低频布局问题；不能掩盖安全、存档或用户误解。

## 7. 发布清单

### 7.1 工程与构建

- [ ] clean checkout + 固定依赖可构建 Windows/macOS；记录完整命令和环境。
- [ ] game/protocol/save/ruleset/content/public schema 版本完整且相互兼容。
- [ ] release 构建没有开发开关、测试固定 seed、调试命令、真实凭据或绝对路径。
- [ ] 导出包包含启动参数说明、网络端口/防火墙说明、保存/备份说明。
- [ ] dedicated 技术包和用户客户端区分；在macOS证明命令行启动/迁移/关服，Linux完整发行后置；不向普通用户承诺未完成的专用服安装体验。

### 7.2 数据与安全

- [ ] 空白世界、示例世界、升级 fixture、迁移 fixture 都通过加载/保存。
- [ ] 最近 5 份自动快照和手动备份轮转符合规则；恢复步骤由非开发者试过。
- [ ] 公开默认关闭；导出前双重授权；HTML/JSON 和撤回用例通过。
- [ ] 敏感文件权限/位置、日志轮转、诊断脱敏和私钥处理经过审阅。
- [ ] 已知房主信任边界、数据回退边界、网络可达边界写进用户文档；包含保存故障暂停、旧备份重新绑定/重新同意和私有迁移包保管说明。

### 7.3 内容与用户体验

- [ ] 五作物、三建设、救济、引导、快捷键、错误/同步/保存状态可理解且无占位素材混入。
- [ ] 所有第三方资产、字体、音效、插件和许可证被登记；没有未经授权的既有作品素材。
- [ ] 5 名新手首轮反馈归档；至少 4 人完成核心流程，阻断/严重缺陷清零。
- [ ] 包含变更日志、已知问题、反馈渠道、支持信息和卸载/数据备份说明。

### 7.4 归档证据

- [ ] 每项 CASE 记录通过版本、环境、日志/截图/录像及失败复现信息。
- [ ] 性能/网络报告包含实测值，不用目标值冒充结果。
- [ ] 构建哈希、安装包、回滚包、存档 fixture、schema、ADR、测试报告和素材清单归档。
- [ ] LEAD 标注 `0.1.0` 的最终提交和后续 V1.1 backlog；未纳入功能不进入变更日志“已支持”。

## 8. 用户交付说明必须说清楚

1. 房主 A 的游戏进程是世界权威；A 退出或崩溃时，其他玩家不能继续推进，可能回退到最近有效保存。
2. 跨家庭网络需要可达地址、正确端口和防火墙设置；V1 不提供官方中继和一键 NAT 配置。
3. 榜单是该世界的合作统计；房主控制存档，V1 不承诺全球官方公平或不可篡改。
4. 公开榜单只在授权后导出；他人已下载的静态文件不能由游戏自动收回。
5. 迁移到团体自建服务器要先停服并导出，不支持同一世界双开或自动合并。
