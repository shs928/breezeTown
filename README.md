# 微风小镇：3D 可玩原型

当前工程使用 Godot 4.7.2。默认世界已切换为按用户确认比例尺描绘的第一张地图：**约 2598 × 2374 米，西南为玩家农场，东北为 NPC 农庄**。地图依据 [first-map-scale.png](docs/art/reference/first-map-scale.png)，其中 400 米标尺长 202 像素，按 `1 px = 400 / 202 m` 换算整张 1312 × 1199 像素图片。约 6.17 平方公里是包含海域的矩形包围范围，不是可耕地面积。

建筑中心、道路、河湖、桥梁与海岸通过同一份 [地图蓝图](game/resources/maps/first_map_blueprint.json)定位。图中的建筑是示意符号，**只按图定位，模型使用合理的实际尺寸**，不把屋顶图标的像素宽度直接当成建筑宽度。图上东北的 “Your Farm” 标签不作为归属规则；玩家农场按用户确认放在西南。比例、描图方法、资产流程和限制详见 [第一张地图说明](docs/maps/first-map.md)。

默认入口仍为 `game/scenes/main.tscn`，`world_builder.gd` 默认转交 `first_map_builder.gd`。旧的约 192 × 160 米近景参考农场保留为 **`--reference-farm`**，用于画风对照与旧场景回归，不是默认地图。2D 历史工程保留在 `backup/2d-game-20260915` 分支。

## 当前实现与边界

- 第一张地图的数据登记包含 9 个区域、22 处建筑、21 条主路、4 段河流、3 片湖塘、4 座桥、9 块区域农田及港口码头。建筑入口支路和港务小屋平台由代码补充。
- 玩家农舍附近保留实际可操作的小块菜园、牧场及树木资源；既有开垦、播种、浇水、收获、买卖、跨天、养殖与存档代码接入新世界。图上的大片农田目前主要是地表表现，不等于每块都已建立完整种植状态。
- 星辉矿场保留十层探索、采矿、战斗、升降机和掉落物系统；入口按新蓝图位于西北。医院、学校、社区中心等多数建筑目前为外观地标，尚无完整室内或 NPC 服务。
- 农舍、商店、谷仓、鸡舍与成年树使用可编辑 Blender 源文件 / GLB 资产；部分乡镇建筑、地形和道具仍由代码生成。木纹、墙面、石材、屋瓦与地表纹理可由脚本重建，HUD 使用参考图派生的木框与纸面、实时文字和工具图标。
- 大图的近处森林按 64 米区块加载，水岸碰撞与寻路限定计算范围。这是控制大地图开销的实现措施，尚不能据此宣称性能、全图连通性或长时间运行已验收。

**仍需完成**：参考图整体视觉匹配、远景森林与地形细节、建筑比例和门前空间逐栋核查、大尺度地图的移动节奏、全图路线验收、NPC 日程与任务、完整季节天气玩法、音效和联机。当前画风仍在迭代，贴图与 GLB 接入不代表已达到参考图质量。

## 运行与操作

使用 Godot 编辑器打开 `game/project.godot`，先完成资源导入再运行。macOS 仓库内引擎示例：

```bash
# 默认：真实比例第一张地图
tools/engine/Godot-4.7.2-stable/Godot.app/Contents/MacOS/Godot --path game
# 旧近景参考农场
tools/engine/Godot-4.7.2-stable/Godot.app/Contents/MacOS/Godot --path game -- --reference-farm
```

Windows 可使用同目录内 `Godot_v4.7.2-stable_win64.exe`，无界面检查使用对应 `_console.exe`。操作：**WASD 移动、Shift 快跑、滚轮缩放、M 地图、1–9 工具、R 换种子、E 交互、Tab 背包、Q 食用口粮、F5 存档、F9 读档、Esc 关闭面板**。工具顺序为空手、锄头、水壶、种子、围栏、镐子、短剑、斧头、树苗。矿场可用 E、空格或鼠标左键采矿/攻击。

睡觉自动保存到 `user://saves/slot_1/`，启动参数 `--load` 可请求读档。默认大图与旧参考农场使用不同地图 ID；旧坐标存档不能视为已完成迁移。请以当前存档校验结果为准，不能直接把旧原型的恢复结论用于新图。

以下是可重复执行的检查入口，**列出命令不代表本次执行或通过**；新图运行证据由对应检查日志和实机截图记录：

```bash
# 第一张地图：标尺、归属、建筑尺度、入口、基础交互及加载范围
tools/engine/Godot-4.7.2-stable/Godot.app/Contents/MacOS/Godot --headless --path game --script res://scripts/tests/first_map_checks.gd
# 领域层：数据、农场、时钟、经济与存档
tools/engine/Godot-4.7.2-stable/Godot.app/Contents/MacOS/Godot --headless --path game --script res://scripts/tests/core_checks.gd
# 默认地图实机截图；路径使用绝对路径
tools/engine/Godot-4.7.2-stable/Godot.app/Contents/MacOS/Godot --path game -- --shot=/tmp/breezetown-first-map.png
# 默认地图总览
tools/engine/Godot-4.7.2-stable/Godot.app/Contents/MacOS/Godot --path game -- --view=overview --shot=/tmp/breezetown-first-map-overview.png
```

截图还支持 `--view=map`、`--at=x,z`、`--zoom=0.8/1.0/1.35/1.8/2.3`、`--hour=22`；`--mine=1` 至 `--mine=10` 可直接进入矿层。旧版 [山谷验收](docs/testing/3d-valley-map.md)、[矿场验收](docs/testing/3d-mine.md)描述的是当时版本，不自动构成本次大图的验收证据。

## 架构与美术资料

[V2 PRD](docs/product/01-product-prd-v2.md)是产品方案，[M0 审计](docs/execution/m0-audit.md)记录此前的基础重构。数据目录在 `game/scripts/data/`；农田、动物、玩家经营状态与 3D 节点分离；`core/` 提供时钟、事件、存档、地图规则、交互和寻路服务。

- [第一张地图](docs/maps/first-map.md)：比例换算、人工描图、默认入口、资产重建、限制与验收入口。
- [Art Bible](docs/art/art-bible.md)：目标画风、当前实现与差距。
- [素材来源与许可](docs/art/assets-license.md)：原创生成资产、用户提供参考图、派生 HUD 与地图资源、字体。
- `art/3d/source/`：当前 Blender 可编辑资产；`art/2_5d/`：保留的旧美术工坊。

下方全部为 2D 时期的历史任务记录，不表示当前 3D 原型已经达到对应交付状态。

## 历史任务包

文档日期：2026-09-14（Asia/Shanghai）  
需求基线：0.4；本次更新实现与验证状态，技术契约沿用已记录的冻结版本与增量  
游戏名称：微风小镇（用户于 2026-09-08 确定）  
目标版本：V1 / 游戏版本 0.1.0，可交付给熟人团体封闭测试的完整小型作品

## 先读这段

**当前主线是上面的 3D 可玩原型。** 以下引用的旧手册与任务板保留 2D 时期状态；当前 3D 资料以前述入口、`docs/maps/`、Art Bible 与 V2 PRD 为准。2D 工程快照位于 `backup/2d-game-20260915` 分支。

这套文件用于把一个项目交给多名 agent 协作，不是让每名 agent 各做一款游戏。

- 用户已经确认：养成/农场方向、多人共享世界、玩家 A 当服务器、世界内贡献和收益榜、未来团体自建服务器、渐进迭代与清晰解耦。
- 《微风小镇》第一版以镇上的公共农场为游玩区域；名称不增加 NPC 日程、自由建城或多地图等首版范围。
- 地图大小、人数上限、共享资金、作物数量、平台和技术栈等是本任务包提出的默认方案，不冒充用户已经确认的决定。
- 2026-09-08 用户已授权开始执行；当前继续完成可玩性修复与重新验收，真实状态见 [执行任务板](docs/execution/task-board.md)。
- M0 风险实验和模块测试是基础证据，正式入口、持续运行和玩家体验需要各自验证。不要把“预留扩展点”变成提前开发未来所有功能。
- “第一版成品”不等于《星露谷物语》的内容规模，也不等于已经满足商店上架条件。

## 文档导航

| 文件 | 用途 | 谁必须读 |
|---|---|---|
| [docs/legacy/2d-product-prd.md](docs/legacy/2d-product-prd.md) | （已归档）2D 时期目标、范围、玩法规则 | 仅历史参考 |
| [docs/legacy/2d-architecture.md](docs/legacy/2d-architecture.md) | （已归档）2D 模块、协议、存档与兼容契约 | 仅历史参考 |
| [docs/legacy/2d-roadmap.md](docs/legacy/2d-roadmap.md) | （已归档）2D 阶段门禁与任务清单 | 仅历史参考 |
| [docs/legacy/2d-acceptance.md](docs/legacy/2d-acceptance.md) | （已归档）2D 验收场景与发布清单 | 仅历史参考 |
| [docs/legacy/2d-execution-readiness.md](docs/legacy/2d-execution-readiness.md) | （已归档）2D 审查结论与开工任务卡 | 仅历史参考 |

冲突处理优先级：用户新决定 → 已批准变更记录 → 产品 PRD → 技术契约 → 任务拆解 → 测试脚本。发现冲突必须提出，不允许各模块自行解释后继续实现。

## 第一版的一句话定义

一款原创 2D 俯视角合作农场游戏：1 人可玩、2–4 人联机；房主保存世界，大家种植、浇水、收获、出售、完成公共建设；游戏内显示累计收益、当日收益和贡献榜，并可经授权导出公开榜单快照。

第一版要验证的产品目标：朋友能进入同一个农场，完成一次分工种植和共同建设，看懂个人贡献与团队资金，并在退出后继续同一份进展。交付终点是通过验收的 0.1.0 封闭测试包、操作手册和测试证据；M0 技术实验、M1 单人切片都是中间成果。

## 初始分派记录（历史）

1. 主执行者承担 LEAD，已记录角色分配；先推进 M0 风险实验与契约冻结。
2. 按 `05-execution-readiness.md` 的已填任务卡先执行 `FND-01`，可同时执行 `UI-01`。引擎基线完成后，再分派 `FND-02`、`NET-01`、`DATA-00`、`QA-01`；根据可用人数分批，实验使用独立目录。
3. LEAD 汇总上述证据完成 `LEAD-01`，冻结版本、接口、存档模型与写入范围后，再分派正式领域与存储实现。
4. 后续最多同时推进 3–5 个独立实现任务。缺少依赖的 agent 先使用契约样例，不自行创造另一套协议。
5. 将任务文件中的分派模板复制给对应 agent；不要只发一句“按 PRD 开始开发”。

## 必须守住的红线

- 房主玩家与远端玩家经过同一业务校验，不直接从 UI 改背包、金币或贡献。
- 默认共享资金；“累计收益”表示归属于出售者的毛销售额，不是个人钱包或净利润。
- 一次收获、一笔出售、一次捐赠都只能结算一次；物品、资金和统计必须来自同一次业务提交。
- 今日按游戏日，不按用户电脑时区；房主离线不推进世界。
- 私有存档与公开榜单使用不同数据结构；公开默认关闭，玩家行默认不对外发布。
- 自建服务器第一版只要求无界面运行与迁移技术验证，不承诺完整托管平台。
- 无自动接管房主、无全球官方公平榜、无素材侵权、无把不通过的测试写成已通过。

## 交付与状态

安全基线与 R1/R2 的主要实现已接通；R3 已补人物动画、七作物阶段、农务音效、指引和实际设置，R4 已补真实同步、重连、审批与 dedicated。R3 的玩法平衡、R4 的跨机长稳以及 R5 的恢复副本界面、双平台新包、性能和真人验收仍需推进。M1–M6 的既有目标继续保留，不能把历史 ACCEPTED 或自动化通过当作玩家验收。12–20 周仅为原有人员/设备假设下的参考；未记录证据的时间、性能、玩法时长均不是实测结果。

本包覆盖 17 条功能需求、8 条非功能要求、M0–M6 七个阶段、45 个任务和 49 个验收场景。

当前这六份文档位于项目根目录，作为唯一需求基线；工程创建后新增决策和报告放入 `docs/`，不复制另一份 PRD。若未来迁移基线文档，由 LEAD 一次性迁移并更新全部链接和任务入口。每次需求变更记录原因、影响范围、负责人、批准状态和版本。
